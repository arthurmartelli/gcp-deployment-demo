# GCP Deployment Demo

Simple deployment demo to create an application in App Engine and GKE

```bash
export REGION=""
git clone https://github.com/arthurmartelli/gcp-deployment-demo
cd gcp-deployment-demo && ./gke-cluster.sh
cd gcp-deployment-demo && ./gcp-deployment-demo.sh -i
```
